# B Active catalogue presentation

Implementation candidate for issue #48. Mode: Persuade for collections/editorial; Operate for choosing a purchasable variation. Extends the approved ivory/sage world with Rajdhani 600 headings and Inter supporting text. This record does not establish deployment or product acceptance.

## Collection
Open portrait image stages at 3:4 preserve complete original garments with object-fit contain. Names, category metadata and native prices/actions remain HTML. No quick-add or stock inference is introduced. Only release-allowlisted product classes receive these styles. Older production custom.css has broad important rules, so the new modules use narrowly scoped overrides for their cards/buttons. Global CSS is preserved.

## Editorial
The existing homepage fit group is restyled only when its serialized block SHA-256 matches the reviewed source. No duplicate section or page-content rewrite is introduced. The large image/text composition stacks image then text below 700px; containment preserves the complete garment. The current factual copy and size-guide link remain intact. A separate editorial switch and optional validated image attachment allow independent activation and reversal. The older explicit shortcode is retained for compatibility but is not used for this homepage release.

The first candidate uses Court Dress attachment387 as its source. Independent review accepts approximate garment fidelity for editorial use, with regenerated details; it is not physical shade certification. The candidate remains outside the production attachment manifest.

## Product selection
Versioned, exact-product activation enhances native WooCommerce colour/size selects. Named buttons have minimum44px targets, pressed states and visible focus. Colour circles require approved hex and matching taxonomy term ID; omitted/unapproved shades remain named controls. WooCommerce continues to own available combinations, selected variation ID, AJAX, images, price and add-to-cart. An explicit dropdown fallback preserves selections, and initialization failures restore native controls.

The product layout uses a large native Blocksy gallery with a compact purchasing column on desktop, then gallery followed by controls on mobile. Existing thumbnails gain keyboard activation through their native click behavior and remain synchronized after WooCommerce replaces the gallery. The original short description moves intact into a reversible Details & fit disclosure; native care/review/shipping tabs and the matching size guide remain available. Woo image updates, lightbox, pricing and cart matching remain native. Browser qualification is still pending; source and DOM tests do not prove rendered image synchronization.

## Gates and recovery
All modules default off. Each surface has its own activation switch. Reviewed palettes require a separate exact-true per-product review flag, valid six-digit hex and matching taxonomy IDs; card previews do not depend on product enhancements being enabled. Held entries have empty palettes and remain unreleased.

A fresh encrypted off-server backup, actual contained restore, 253-variation Woo cart matrix and targeted image/design-file rollback drills passed on September 12. The isolated browser is blocked by Chrome, so visual, keyboard, responsive-image and live verification remain outstanding. See the current work record and product checklist for the exact acceptance state. Do not infer deployment from local code or test results.
