# Bind over /etc/resolv.conf so Docker's embedded resolver cannot forward queries.
# There is no DNS server on container loopback. Local DB lookup uses /etc/hosts.
nameserver 127.0.0.1
options timeout:1 attempts:1
